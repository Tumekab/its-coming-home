const GenerateTeams = ({ handleClick }) => {
    return (
        <button 
            className="btn generateBtn"
            onClick={ handleClick }>
            generate teams
        </button>
    )
}

export default GenerateTeams;