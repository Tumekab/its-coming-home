const GenerateTeams = ({ handleClick }) => {
    return (
        <button 
            className="btn-settings teams-button"
            onClick={ handleClick }>
            generate teams
        </button>
    )
}

export default GenerateTeams;